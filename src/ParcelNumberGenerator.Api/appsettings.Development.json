{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },

  "//": [
    "No connection string here on purpose: with none, the service falls back to the",
    "in-memory provider, so `dotnet run` works on a fresh clone with nothing installed.",
    "The AppHost supplies a real Postgres connection string when you run that instead.",
    "No Jwt section either — the endpoints are open in Development and the startup guard",
    "only requires an issuer in Production."
  ],

  "Pool": {
    "From": 1,
    "To": 99999,
    "Exclusions": [
      { "From": 30000, "To": 40000 }
    ]
  }
}
