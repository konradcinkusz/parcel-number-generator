{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": [
    "P5 — shape and non-secret defaults only. The backend addresses resolve through the",
    "proxy's candidate ladder: Services__Generator__BaseUrl / Services__Notifications__BaseUrl",
    "when set (the platform rung), the AppHost's service-discovery variables under Aspire,",
    "and localhost ports 5180/5181 as the bare-laptop fallback."
  ],

  "Services": {
    "Generator": { "BaseUrl": "" },
    "Notifications": { "BaseUrl": "" }
  }
}
