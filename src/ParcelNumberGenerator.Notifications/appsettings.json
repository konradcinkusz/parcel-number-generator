{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": [
    "P5 — this file carries shape and non-secret defaults only. Every environment-specific",
    "value arrives as an environment variable with __ as the separator (Jwt__Authority,",
    "ConnectionStrings__notificationsdb). No secret is ever a literal here.",
    "flyio/SECRETS.md names every variable, its tier, and where its value comes from."
  ],

  "Jwt": {
    "Authority": "",
    "Audience": "png-notifications"
  },

  "Cors": {
    "AllowedOrigins": []
  },

  "Notifications": {
    "Webhook": {
      "//": "Optional (P8). Unset, notifications are logged rather than pushed.",
      "Endpoint": ""
    }
  }
}
