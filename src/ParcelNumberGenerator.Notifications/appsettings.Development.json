{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },

  "//": [
    "Development defaults, so `git clone && dotnet run` produces a working system with no",
    "cloud credentials (P8). No issuer is configured, so the endpoints are open — the same",
    "posture as the generator API in development, enforced against production by the",
    "startup guard. Set Jwt__Authority here the day the estate has an identity service."
  ]
}
